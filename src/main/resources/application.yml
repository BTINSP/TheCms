spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/springdb?serverTimezone=UTC
    hikari:
      max-lifetime: 200000
  thymeleaf:
    encoding: UTF-8
    prefix: classpath:/templates/
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.thecms.entity
server:
  port: 8080